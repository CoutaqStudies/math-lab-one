#include <cstdint>;
#include <math.h>
#include "Factorial.cpp"
#include <numbers>
class ErrorFunction {
public:
    static double CalculateError(uint64_t n) {
        double exp = 2 / sqrt(std::numbers::pi);
        double n = 0;
    }
};